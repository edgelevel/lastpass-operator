apiVersion: niqdev.com/v1alpha1
kind: LastPassSecret
metadata:
  name: example-lastpass-secret
spec:
  itemRef:
    # optional (default "")
    group: example
    # required
    name: example-pwd
    # optional (default true)
    withUsername: true
    # optional (default true)
    withPassword: true
    # optional (default true)
    withUrl: true
    # optional (default true)
    withNote: true
